apiVersion: v1
kind: Service
metadata:
  name: halo-downpatch-web
  namespace: downpatch-tool-halo
spec:
  type: ClusterIP
  selector:
    app: halo-downpatch-web
  ports:
    - name: http
      port: 80
      targetPort: 80